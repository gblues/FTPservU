#include "commands.h"
#include "ftp.h"

/**
 * Accept the specified file from the client. If the file exists,
 * the data is appended to the existing file; the file is created if
 * it does not exist.
 */
void do_append(client_t *client, char *args)
{
}
